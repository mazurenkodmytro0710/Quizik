# Тестові питання — Нумерична математика та Теорія ймовірностей

> ✅ = правильна відповідь | ⚠️ = може бути декілька правильних

---

## 1. Pre rovnicu f(x) = 0 a pre číslo α platí: f(α) = f′(α) = f′′(α) = 0 a f′′′(α) ≠ 0. Potom:

a) α je jednoduchý koreň rovnice f(x) = 0,
b) α je dvojnásobný koreň rovnice f(x) = 0,
✅ c) α je trojnásobný koreň rovnice f(x) = 0,
d) α je viac ako trojnásobný koreň rovnice f(x) = 0.

---

## 2. Pre rovnicu f(x) = 0 a pre číslo α platí: f(α) = f′(α) = 0, f′′(α) ≠ 0 a f′′′(α) ≠ 0. Potom:

a) α je jednoduchý koreň rovnice f(x) = 0,
✅ b) α je dvojnásobný koreň rovnice f(x) = 0,
c) α je trojnásobný koreň rovnice f(x) = 0,
d) α je viac ako trojnásobný koreň rovnice f(x) = 0.

---

## 3. Pri riešení rovnice f(x) = 0 iteračnou metódou sa hodnoty x₁, x₂, x₃, … približujú ku koreňu rovnice:

a) vždy iba z jednej strany,
b) vždy striedavo z oboch strán,
✅ c) v závislosti od iteračného predpisu sa približujú buď iba z jednej strany alebo iba striedavo z oboch strán,
d) v závislosti od iteračného predpisu môžu sa chvíľu približovať z jednej strany a potom preskočiť na druhú stranu od koreňa.

---

## 4. Výber hlavného prvku pri riešení sústavy lineárnych rovníc je súčasťou:

a) iteračnej metódy,
✅ b) Gaussovej eliminačnej metódy,
c) Newtonovej metódy,
d) metódy rozkladu matice sústavy na súčin trojuholníkových matíc.

---

## 5. Ak pre náhodnú premennú X platí, že M₀(X) = {2; 3}, tak:

a) náhodná premenná X sa určite riadi binomickým rozdelením pravdepodobnosti,
b) náhodná premenná X sa určite riadi iným ako binomickým rozdelením pravdepodobnosti,
✅ c) existuje viac typov náhodnej premennej, pre ktorú to môže platiť,
d) neexistuje taká náhodná premenná.

---

## 6. Ak pre náhodnú premennú X platí, že E(X) = {2; 3}, tak:

a) náhodná premenná X sa určite riadi binomickým rozdelením pravdepodobnosti,
b) náhodná premenná X sa určite riadi iným ako binomickým rozdelením pravdepodobnosti,
✅ c) existuje viac typov náhodnej premennej, pre ktorú to môže platiť,
d) neexistuje taká náhodná premenná.

---

## 7. Vzťah P(A ∪ B) = P(A) + P(B) − P(A ∩ B):

a) platí iba pre všetky nezávislé javy A a B,
b) platí iba pre nezlúčiteľné javy A a B,
✅ c) platí pre ľubovoľné javy A a B,
d) neplatí.

---

## 8. Vzťah P(A ∪ B) = P(A) + P(B) + P(A ∩ B):

a) platí iba pre všetky nezávislé javy A a B,
b) platí iba pre nezlúčiteľné javy A a B,
c) platí pre ľubovoľné javy A a B,
✅ d) neplatí.

---

## 9. Ak náhodná premenná X sa riadi exponenciálnym rozdelením pravdepodobnosti s parametrom λ > 0, tak: (mód vs. stredná hodnota)

a) E(X) > λ,
b) M₀(X) > E(X),
✅ c) M₀(X) = 0,
d) M₀(X) = 1/λ.

*(Pre exponenciálne rozdelenie je hustota f(x) = λ·e^(−λx) na x ≥ 0 klesajúca, preto mód leží v bode 0. Hodnota 1/λ je stredná hodnota E(X).)*

---

## 10. Ak náhodná premenná X sa riadi exponenciálnym rozdelením pravdepodobnosti s parametrom λ > 0, tak: (porovnanie E, D, σ)

a) E(X) > D(X),
b) E(X) < D(X),
✅ c) E(X) = σ(X),
d) E(X) = λ².

*(Pre exponenciálne rozdelenie: E(X) = 1/λ, σ(X) = 1/λ, teda E(X) = σ(X))*

---

## 11. Interpolačný polynóm a funkcia, ktorú aproximuje:

a) v uzlových bodoch interpolácie môžu mať rovnaké funkčné hodnoty,
✅ b) v uzlových bodoch interpolácie musia mať rovnaké funkčné hodnoty,
c) nemajú rovnaké funkčné hodnoty v žiadnom uzlovom bode interpolácie,
d) majú rovnaké funkčné hodnoty na celom interpolačnom intervale.

---

## 12. Pre nezlučiteľné javy A a B určite platí:

✅ a) P(A ∪ B) = P(A) + P(B),
b) P(A ∪ B) = P(A) · P(B),
c) P(A ∩ B) = P(A) + P(B),
d) P(A ∩ B) = P(A) · P(B).

---

## 13. Pre nezávislé javy A a B platí:

a) P(A ∪ B) = P(A) + P(B),
b) P(A ∪ B) = P(A) · P(B),
c) P(A ∩ B) = P(A) + P(B),
✅ d) P(A ∩ B) = P(A) · P(B).

---

## 14. Sústava m lineárnych rovníc o n neznámych môže: ⚠️

✅ a) mať jediné riešenie,
b) mať n riešení,
c) mať viac ako jedno riešenie, ale konečne veľa,
✅ d) nemať riešenie.

*(Sústava môže mať jediné riešenie, nekonečne veľa riešení alebo nemať riešenie vôbec)*

---

## 15. Binomické rozdelenie náhodnej premennej sa vyskytuje pri:

a) závislých opakovaných pokusoch,
✅ b) nezávislých opakovaných pokusoch,
c) pri podmienených pokusoch,
d) pri ľubovoľných pokusoch.

---

## 16. Pri výpočte integrálu ∫(x₀→xₙ) f(x)dx pomocou Simpsonovho pravidla sa dosiahne presnejší výsledok pre:

a) n = 10 pri nekonštantnom kroku,
b) n = 15 pri konštantnom kroku,
✅ c) n = 12 pri konštantnom kroku,
d) dostatočne veľké n pri ľubovoľnom kroku.

*(Simpsonovo pravidlo vyžaduje párne n; n=12 je párne a má konštantný krok)*

---

## 17. Nech funkcia f(x) je spojitá na intervale ⟨a; b⟩. Ak f(a)·f(b) > 0, tak rovnica f(x) = 0, ktorá nemá násobné korene, má:

a) v intervale ⟨a; b⟩ práve jeden koreň,
b) v intervale ⟨a; b⟩ aspoň jeden koreň,
✅ c) v intervale ⟨a; b⟩ iba párny počet koreňov (aj nula je párne číslo),
d) v intervale ⟨a; b⟩ ľubovoľný konečný počet koreňov.

---

## 18. Ak f(a)·f(b) < 0, tak pre spojitú funkciu f rovnica f(x) = 0, ktorá nemá násobné korene, má v intervale ⟨a; b⟩:

a) práve jeden koreň,
b) párny počet koreňov,
✅ c) nepárny počet koreňov,
d) nevieme rozhodnúť bez poznania funkcie f.

*(Zo spojitosti a zmeny znamienka vyplýva aspoň jeden koreň; bez násobných koreňov ich musí byť nepárny počet.)*

---

## 19. Pri riešení rovnice f(x) = 0 iteračnou metódou na intervale ⟨a; b⟩ iteračný proces xₙ₊₁ = φ(xₙ) konverguje:

a) vždy pri ľubovoľnej voľbe x₀,
b) stačí, ak |φ′(a)| < 1 aj |φ′(b)| < 1,
✅ c) ak |φ′(x)| < 1 pre každé x ∈ ⟨a; b⟩, pričom x₀ ∈ ⟨a; b⟩,
d) ak |φ′(x)| < 1 pre každé x ∈ ⟨a; b⟩, pričom x₀ je ľubovoľné reálne číslo.

---

## 20. Pri riešení rovnice f(x) = 0 sa metóda delenia intervalu dá použiť:

a) vždy,
b) ak f′ existuje na celom separačnom intervale,
c) iba ak f je klesajúca funkcia na celom separačnom intervale,
✅ d) ak je f spojitá na celom separačnom intervale a f(a)·f(b) < 0.

*(Metóda delenia intervalu nepotrebuje deriváciu; stačí spojitosť a zmena znamienka na koncoch intervalu.)*

---

## 21. Pri použití geometrickej definície pravdepodobnosti:

a) nemožný jav môže mať nenulovú pravdepodobnosť,
b) možný jav má vždy nenulovú pravdepodobnosť,
✅ c) možný jav môže mať nulovú pravdepodobnosť,
d) každý jav má nenulovú pravdepodobnosť.

---

## 22. Rozptyl je rovný druhej mocnine strednej hodnoty:

a) pri normálnom rozdelení pravdepodobnosti náhodnej premennej,
b) pri Poissonovom rozdelení pravdepodobnosti náhodnej premennej,
✅ c) pri exponenciálnom rozdelení pravdepodobnosti náhodnej premennej,
d) iba pri diskrétnom rozdelení pravdepodobnosti náhodnej premennej.

*(Pre exponenciálne rozdelenie: D(X) = 1/λ² = [E(X)]²)*

---

## 23. Pre normálne rozdelenie pravdepodobnosti náhodnej premennej vieme určiť hodnotu distribučnej funkcie: ⚠️

a) analytickým výpočtom pre každú hodnotu náhodnej premennej,
✅ b) pre každú hodnotu náhodnej premennej vieme približne určiť hodnotu distribučnej funkcie pomocou numerického integrovania,
✅ c) z tabuliek pre vybrané hodnoty náhodnej premennej, ale iba pre normované rozdelenie,
d) iba pre niektoré hodnoty náhodnej premennej.

---

## 24. Sústava dvoch nelineárnych rovníc:

a) má vždy práve jedno riešenie,
b) má vždy aspoň jedno riešenie,
✅ c) počet riešení závisí od konkrétnej sústavy,
d) má vždy konečný počet riešení.

---

## 25. Pre konštanty c a d a náhodné premenné X a Y platí:

a) E(c·X + d·Y) = E(c)·E(X) + E(d)·E(Y),
✅ b) E(c·X + d·Y) = c·E(X) + d·E(Y),
c) E(c·X + d·Y) = E(c) + E(X) + E(d) + E(Y),
d) E(c·X + d·Y) = (c + d)·(E(X) + E(Y)).

---

## 26. Vzťah P(A ∩ B ∩ C) = P(A) · P(B|A) · P(C|A ∩ B):

*(Nemá varianty odpovede v teste)*
✅ Správna odpoveď: platí pre ľubovoľné javy A, B a C (ide o vzorec úplnej pravdepodobnosti / reťazové pravidlo)

---

## 27. Je možné, aby nejaká funkcia zadaná tabuľkou hodnôt mohla byť aproximovaná metódou najmenších štvorcov funkciou v tvare y = x·ln a + ln b, pričom a a b sú konštanty?

*(Nemá varianty odpovede v teste)*
✅ Správna odpoveď: **áno** — ide o lineárnu závislosť y = ln(a)·x + ln(b), ktorú MNŠ vie spracovať

---

## 28. Lagrangeov interpolačný polynóm je možné použiť na aproximáciu funkcie:

*(Nemá varianty odpovede v teste)*
✅ Správna odpoveď: aj keď sú hodnoty nezávislej premennej zadané **s nerovnakým krokom**

---

## 29. Ostré ohraničenie pre najväčšiu hodnotu diskrétnej náhodnej premennej má:

*(Nemá varianty odpovede v teste)*
✅ Správna odpoveď: **binomické rozdelenie** a **hypergeometrické rozdelenie** pravdepodobnosti náhodnej premennej

---

## 30. Pri riešení rovnice f(x) = 0 metódou regula falsi sa hodnoty x₁, x₂, x₃, … približujú ku koreňu rovnice:

✅ a) vždy iba z jednej strany,
b) iba z jednej strany, ak f′(x) a f″(x) nemení znamienko na separačnom intervale,
c) striedavo z oboch strán.

*(Potvrdené fotografiami zo skúšok RT3-16, A-12, PT-15)*

---

## 31. Pri riešení rovnice f(x) = 0 pomocou Newtonovej metódy sa hodnoty x₁, x₂, x₃, … približujú ku koreňu rovnice:

*(Nemá varianty odpovede v teste)*
✅ Správna odpoveď: **vždy iba z jednej strany** (závisí od konkrétnej funkcie — možno)

---

## 32. Ak f(a)·f(b) > 0, tak rovnica f(x) = 0 v intervale ⟨a; b⟩ (pre spojitú f bez násobných koreňov): ⚠️

*(Z fotogrfií — niektoré varianty odpovede)*
a) určite nemá koreň,
✅ b) určite má párny počet koreňov,
c) ak má koreň a funkcia f je spojitá na ⟨a; b⟩ → má párny počet koreňov.

---

## 33. (Z fotografie — skúška s príkladmi) Ak f(a)·f(b) < 0, tak pre spojitú funkciu f rovnica f(x) = 0, ktorá nemá násobné korene, má v intervale ⟨a; b⟩:

a) práve jeden koreň,
b) párny počet koreňov,
✅ c) nepárny počet koreňov,
d) nevieme rozhodnúť bez poznania funkcie f.

---

## 34. (Z fotografie) Pri riešení rovnice f(x) = 0 metódou sečníc (regula falsi) sa hodnoty x₁, x₂, x₃, … približujú ku koreňu rovnice:

✅ a) vždy iba z jednej strany,
b) iba z jednej strany, ak f′(x) > 0,
c) striedavo z oboch strán.

---

## 35. (Z fotografie — skúška) Je možné aproximovať funkciu z tabuľky metódou najmenších štvorcov funkciou v tvare y = x·ln a + ln b?

a) áno,
b) nie.
✅ Správna odpoveď: **áno**

---

*Spolu: 35 unikátnych otázok z oboch dokumentov (PDF text + skeny/fotografie).*
